# Dev: để trống hoặc không tạo file — Chatbot dùng `/api/chat` (proxy Vite, xem vite.config.js).
# Production: URL đầy đủ tới POST /api/chat (nên dùng 127.0.0.1 hoặc domain thật, tránh localhost nếu gặp lỗi IPv6).
# VITE_CHATBOT_API_URL=http://127.0.0.1:8000/api/chat
